import java.util.*;
public class Pet{
    public static void main(String [] args){
        //code goes here
        Scanner input = new Scanner(System.in);

        String pet;
        System.out.println("Enter your favorite color (Either red, blue or green): ");
        String favoriteColor = input.nextLine();
        System.out.println("Enter your favorite season (winter, spring, summer, fall): ");
        String favoriteSeason = input.nextLine();
        System.out.println("Enter your name: ");
        String name = input.nextLine();
        String consonant = "bcdfghjklmnpqrstvwxyz"
        boolean isThereAConsonant = false
         if(name[0] != "a" && s != "e" && s != "i" && s != "o" && s != "u")
        {
            isConsonant = true;
        } else {
            isConsonant = false;
        }


          if (favoriteColor == "blue")
          {
            if (favoriteSeason == "fall")
            {
                String pet == "alligator";
            }
            else
            {
                if (favoriteSeason == "spring")
                {
                    String pet == "ostritch";
                }
                else
                {
                    if (favoriteSeason != "summer" && isThereAConsonant == true)
                    {
                        if (pet != "ostritch")
                        {
                            pet == "axolotl"
                        }
                    }
                }
            }
          }
    }
}
